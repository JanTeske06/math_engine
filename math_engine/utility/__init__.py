"""
Utility sub-package for math_engine.

Contains shared modules used across the package:

- :mod:`error`              — custom exception hierarchy and error code catalog
- :mod:`config_manager`     — JSON-based persistent configuration management
- :mod:`utility`            — lightweight type-checking and bracket helpers
- :mod:`non_decimal_utility` — non-decimal parsing, conversion, and bit operations
- :mod:`plugin_manager`     — plugin loading framework (work in progress)
"""